---
title: "Laravel常用命令行中文版"
description: "0、new一个Laravel项目 1、生成控制器 对应的会在app/http/controllers下面生成 ArticleController.php 这个控制器。 2、使用RESTFUL生成控制器，这个很有用哦！ 为什么用resource参数，因为我们在做一个后台的时候，设计到增删改查操作，而这些操作我们通过–resource就可以直接生成"
date: "2018-11-05 09:30:31"
tags: ["laravel","php"]
category: "PHP 与 ThinkPHP"
---
> **历史博客说明**
>
> 本文为 Leroi 的历史博客文章，原发布于 CSDN，现迁移并重新整理到本站。
>
> 原博客地址（CSDN）：[https://blog.csdn.net/Leroi_Liu/article/details/83743661](https://blog.csdn.net/Leroi_Liu/article/details/83743661)

## 0、new一个Laravel项目

~~~bash
composer create-project laravel/laravel project-name --prefer-dist "5.5.*"
~~~

## 1、生成控制器

~~~bash
php artisan make:controller ArticleController
~~~

对应的会在`app/http/controllers`下面生成`ArticleController.php`这个控制器。

## 2、使用RESTFUL生成控制器，这个很有用哦！

~~~bash
php artisan make:controller ArticleController --resource
~~~

~~~php
Route::resource('article', 'ArticleController');
~~~

为什么用 `--resource` 参数，因为我们在做一个后台的时候，涉及到增删改查操作，而这些操作可以通过 `--resource` 直接生成对应方法。`Route::resource` 其实就是生成了快捷的资源路由，让我们本来需要写的 `get`、`post`、`delete`、`patch` 直接用 restful 方式一键生成。

### 需要注意的是：`index` 是列表，`store` 是新增，`show` 是详情，`update` 是更新，`destroy` 是删除。

利用命令可以查看当前注册的路由有哪些：

~~~bash
php artisan route:list
~~~

## 3、生成模型

~~~bash
php artisan make:model User -m
~~~

默认的模型文件生成在App目录，如果要生成在其他目录要带目录，比方说我把Model放到了`App/Models/`目录，则执行

~~~bash
php artisan make:model Models/Question -m
~~~

`-m` 参数为生成模型同时生成数据库创建文件。

## 4、PHP数据表维护（迁移）

新建迁移：

~~~bash
php artisan make:migration create_users_table
~~~

新的迁移位于 `database/migrations` 目录下，每个迁移文件名都包含时间戳，从而允许 Laravel 判断其顺序。
`--table` 和 `--create` 选项可以用于指定表名，以及该迁移是否要创建一个新的数据表。这些选项只需要放在上述迁移命令后面并指定表名：

~~~bash
php artisan make:migration add_votes_to_users_table --table=users
php artisan make:migration create_users_table --create=users
~~~

运行迁移：

~~~bash
php artisan migrate
~~~

## 5、填充数据

新建填充：

~~~bash
php artisan make:seeder UserTableSeeder
~~~

运行填充器：

~~~bash
php artisan db:seed
php artisan db:seed --class=UserTableSeeder
~~~

你还可以使用migrate:refresh命令来填充数据库，该命令还可以回滚并重新运行迁移，这在需要完全重建数据库时很有用：

~~~bash
php artisan migrate:refresh --seed
~~~

如果需要插卡全部命令： 
可以运用如下命令：

~~~bash
php artisan
~~~

更加详细的说明可以查看如下链接：

[http://www.golaravel.com/post/laravel-5-0-artisan-cli-options-list/](http://www.golaravel.com/post/laravel-5-0-artisan-cli-options-list/)
