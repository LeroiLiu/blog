---
title: "Dandelion Pro - React Admin Dashboard Template 开发过程中遇见的问题总结"
description: "Dandelion Pro React Admin Dashboard Template 开发过程中遇到的按钮组件、DLL、ESLint 换行和 linebreak-style 问题整理。"
date: "2019-01-18 16:08:18"
tags: ["nodejs","react.js","redux.js","Dandelion"]
category: "前端与 JavaScript"
---

:::note[历史博客说明]
本文为 Leroi 的历史博客文章，原发布于 CSDN，现迁移并重新整理到本站。

原博客地址（CSDN）：[https://blog.csdn.net/Leroi_Liu/article/details/86541540](https://blog.csdn.net/Leroi_Liu/article/details/86541540)
:::

## `fab` variant 将被移除

报错或提示：

```text
Warning: Failed prop type: The `fab` variant will be removed in the next major release. The `<Fab>` component is equivalent and should be used instead.
```

通常出现在头部组件里，例如 `app/components/Header/Header.js`。

原写法：

```jsx
import Button from '@material-ui/core/Button'

<Button
  variant="fab"
  mini
  className={classes.menuButton}
  aria-label="Menu"
  onClick={toggleDrawerOpen}
>
  <MenuIcon />
</Button>
```

替换为：

```jsx
import Fab from '@material-ui/core/Fab'

<Fab
  color="primary"
  size="small"
  className={classes.menuButton}
  aria-label="Menu"
  onClick={toggleDrawerOpen}
>
  <MenuIcon />
</Fab>
```

## DLL manifest 缺失

报错：

```text
The DLL manifest is missing. Please run `npm run build:dll`.
```

处理方式：

```bash
npm run build:dll
```

如果是安装依赖后出现，也要确认依赖是否安装在正确项目里。历史处理里曾经用过全局安装，例如：

```bash
npm i -g @fortawesome/react-fontawesome
```

不过更推荐优先把依赖写入当前项目的 `package.json`。

## 文件末尾缺少空行

报错：

```text
Module Error (from ./node_modules/eslint-loader/index.js):

C:\Users\leroi\Desktop\themeforest-update\dandelion_pro_v130\starter-project\app\containers\pageListAsync.js
59:4 error Newline required at end of file but not found eol-last
```

处理方式：

在提示文件末尾补一个空行，保存后重新运行检查。

## CRLF 和 LF 换行不一致

报错：

```text
error Expected linebreaks to be 'LF' but found 'CRLF' linebreak-style
```

处理方式：

在 `.eslintrc.js` 的 `rules` 中调整换行规则：

```js
module.exports = {
  rules: {
    'linebreak-style': [0, 'error', 'windows']
  }
}
```

如果团队成员同时使用 Windows 和 macOS，更建议配合 `.editorconfig` 统一换行。
